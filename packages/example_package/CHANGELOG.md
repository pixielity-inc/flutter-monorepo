# Changelog

All notable changes to `example_package` are documented here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).
Versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Versions are bumped automatically by `melos version` using
[Conventional Commits](https://www.conventionalcommits.org/).

---

## 0.1.0

### Added

- Initial release.
- `ExampleItem` — immutable domain entity with `freezed`.
- `ExampleRepository` — abstract repository interface.
- `GetExampleItems` — use case for fetching all items.

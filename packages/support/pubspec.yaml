name: pixielity_core
description: >
  Support/utilities package for the Flutter monorepo.
  Provides shared helpers, extensions, and cross-cutting utilities.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  freezed_annotation: ">=2.4.1 <4.0.0"
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.8
  flutter_test:
    sdk: flutter
  freezed: ">=2.4.7 <4.0.0"
  json_serializable: ^6.7.1
  very_good_analysis: ^6.0.0
